import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#199AE2",
      dark: "#006BA7",
    },
    secondary: {
      main: "#57C3FF",
    },
    background: {
      default: "#FFFCF0",
    },
  },
});

export default theme;
