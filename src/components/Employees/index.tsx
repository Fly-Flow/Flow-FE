import { Typography } from "@mui/material";
import SideBar from "../shared/SideBar";

const Employees: React.FC = (props) => {
  return (
    <>
      <SideBar />
      <Typography variant="h4">구성원</Typography>
    </>
  );
};

export default Employees;
